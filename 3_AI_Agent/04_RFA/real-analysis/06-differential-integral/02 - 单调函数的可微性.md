# 单调函数的可微性

## 核心内容
1. **勒贝格微分定理**：定义在区间上的单调函数，几乎处处可微。
2. 单调函数的性质：
   - 不连续点集是至多可数集；
   - 几乎处处存在有限导数。
3. 导数的积分：若 f 是 [a,b] 上的单调函数，则 f' 是勒贝格可积函数，且
   从 a 到 b 的积分 f'(x)dx <= f(b)-f(a)。
4. 典型例子：康托尔函数，单调递增且几乎处处导数为0，但 f(1)-f(0)=1>0。
