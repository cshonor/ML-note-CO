# §4.1 随机变量序列的两种收敛性

## 1. 依概率收敛
若对任意 epsilon>0，
lim_{n->∞} P(|X_n-X|>=epsilon)=0，
则记作 X_n 依概率收敛到 X（X_n ->P X）。

## 2. 依分布收敛（弱收敛）
设 F_n(x) 是 X_n 的分布函数，F(x) 是 X 的分布函数。
若对 F 的每个连续点 x 都有 lim_{n->∞}F_n(x)=F(x)，
则记作 X_n 依分布收敛到 X（X_n ->d X）。
