# §4.2 特征函数

## 1. 定义
phi(t)=E(e^(itX))

## 2. 基本性质
- phi(0)=1，且 |phi(t)|<=1
- 线性变换：若 Y=aX+b，则 phi_Y(t)=e^(itb)*phi_X(at)
- 独立和：若 X,Y 独立，则 phi_{X+Y}(t)=phi_X(t)*phi_Y(t)

## 3. 唯一性定理
特征函数唯一决定分布函数（分布与特征函数一一对应）。
